# Copy to .env and fill in. .env is gitignored and holds every credential this
# project uses — there is no second env file.
#
#   cp .env.example .env
#
# The VITE_ prefix is the security boundary: Vite inlines VITE_-prefixed values
# into the public browser bundle at BUILD time and leaves everything else on the
# server. Never move a secret above that line.

# --- Browser (public, inlined into the bundle) -----------------------------
# Leave both blank to run in "direct" mode — the Vite dev proxy fetches NSE and
# Yahoo itself. That works for `npm run dev` only; a production build needs
# Supabase. Fill them in (Supabase dashboard → Connect → Framework) to switch
# the app over to reading pre-ingested rows from your own Postgres.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Legacy projects issue an `anon` JWT instead of a publishable key. Either works.
# VITE_SUPABASE_ANON_KEY=

# --- Firebase Cloud Messaging (browser half) -------------------------------
# Push alerts: a UT Bot BUY on a screen symbol goes to everyone, a SELL on a
# watchlisted symbol goes to that list's owner. See supabase/functions/
# notify-signals and migration 0016_push.sql.
#
# All five are public — they are inlined into the bundle, which is what VITE_
# means, and Firebase's own docs say so. The thing that authorises a *send* is
# the service account below, which never comes near the browser.
#
# Firebase console → Project settings → General → Your apps → Web app.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Project settings → Cloud Messaging → Web Push certificates → Generate key
# pair. Without it getToken mints a token that FCM v1 silently never reaches.
VITE_FIREBASE_VAPID_KEY=
#
# ⚠ Two more places need these, and neither reads this file:
#   · public/firebase-messaging-sw.js hardcodes the same config. A static file
#     in public/ is copied verbatim, so Vite substitutes nothing into it — if
#     you change a value here, change it there.
#   · Cloudflare needs the VITE_* ones as BUILD variables in the dashboard,
#     the same note wrangler.toml already carries for the Supabase keys.

# --- Firebase (server half). NEVER add a VITE_ prefix here. -----------------
# The send is authorised by a service account, not by these:
#
#   FCM_SERVICE_ACCOUNT  the whole downloaded JSON, as one value
#   APP_URL              absolute https URL of the deployed app; the
#                        notification's click target. Optional — without it the
#                        alert still shows, it just does not navigate. FCM
#                        rejects a relative link, which is why it cannot be
#                        defaulted to '/'.
#
# Both live on the Edge Function, not in this file:
#
#   supabase secrets set FCM_SERVICE_ACCOUNT="$(cat service-account.json)"
#   supabase secrets set APP_URL="https://your-app.example"
#
# Project settings → Service accounts → Generate new private key. That file is
# a credential capable of pushing to every device this app has registered —
# keep it out of the repo.

# --- Server only (scripts/seed.mjs). NEVER add a VITE_ prefix here. ---------
# The seeding script needs a SECRET key, which bypasses RLS — so it must never
# reach the browser. Dashboard → Project Settings → API keys → secret.
# The seeder reuses VITE_SUPABASE_URL above for the host.
SUPABASE_SECRET_KEY=

# --- Upstox (server only). NEVER add a VITE_ prefix here. ------------------
# One credential, and only one: the ANALYTICS TOKEN. Upstox → Apps → My Apps →
# Analytics tab → Generate Token. One year, read-only, one per account, no
# OAuth app to create, no redirect URI, and no static IP to whitelist — which
# matters, because Supabase Edge Functions and Cloudflare Workers have no
# stable outbound IP to whitelist in the first place.
#
# It covers Market Quote, Historical Data, Fundamentals, Market Information and
# the websocket feed. What that buys, and the three places the current Yahoo
# path stays better anyway, is written up in plan.md — the short version is that
# it replaces the /v7 cookie+crumb dance, prices NSE Emerge live (retiring the
# bhavcopy walk-back), and turns the screener.in ROCE scrape from 2.5 days into
# ~90 minutes. It does NOT replace Yahoo `spark`: Upstox has no batch candle
# endpoint, so a whole-universe monthly pass is 5,229 requests against 262.
#
# It does NOT go in this file. The token lives in the database — one row,
# read by the Edge Functions and by the local scripts alike (migration 0011),
# so rotating it is one statement with no deploy and no build:
#
#   select public.upstox_token_set('<token>');
#
# `UPSTOX_ACCESS_TOKEN` is still honoured as a fallback if you set it here or
# via `supabase secrets set`, but only so that migration 0011 is safe to apply
# before a token is stored. Two places to look is how a stale token survives a
# rotation, so once the database has one, don't keep a second copy.
#
# No token at all is a supported state: every Upstox path degrades to the Yahoo
# one, and the app runs unchanged without it.

# --- The OAuth path, and why it is not here ---------------------------------
# Upstox's other credential is an OAuth token that expires at 3:30 AM IST every
# day and issues no refresh token, so something has to log in each morning or
# every pg_cron schedule breaks until someone does it by hand.
#
# This project used to carry that: a GitHub Actions workflow that logged in
# nightly with a stored password, PIN and TOTP seed. It is deleted (see git
# history for `.github/workflows/upstox-token.yml`), because the Analytics Token
# above made it dead weight — and it was the only thing in this repo that needed
# credentials capable of placing a trade.
#
# `UPSTOX_API_KEY`, `UPSTOX_API_SECRET` and `UPSTOX_REDIRECT_URI` went with it.
# No code reads them. If the Analytics tab is ever unavailable on your account,
# recover that workflow from git rather than re-deriving it.
